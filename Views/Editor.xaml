<UserControl x:Class="MCAddonSetupManager.Editor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="root">
    <DockPanel >
        <!--<TreeView DockPanel.Dock="Left" MinWidth="100">
        </TreeView>-->
        <Grid>
            <StackPanel >
                <TextBlock Text="{Binding Text, ElementName=root, StringFormat='{}{0} : '}"/>
                <TextBox Text="{Binding Value, ElementName=root}"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</UserControl>
